import { ColorSortPipe } from './color-sort.pipe';

describe('ColorSortPipe', () => {
  it('create an instance', () => {
    const pipe = new ColorSortPipe();
    expect(pipe).toBeTruthy();
  });
});
